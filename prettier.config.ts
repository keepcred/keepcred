import type { Config } from 'prettier'

/**
 * Конфигурация Prettier
 */
const config: Config = {
  singleQuote: true,
  semi: false,
  printWidth: 100,
  trailingComma: 'none'
}

export default config
